import logo from '../assets/logo.svg';

const Header = () => {
  return(
    <header className="App-header">
      <img src={logo} alt="Qué bonito logo" width="424" height="243" />
    </header>
  );
}

export default Header;
